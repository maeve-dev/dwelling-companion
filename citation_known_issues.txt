# citation_known_issues.txt -- baseline for check_citations.py
# Dated: 2026-09-15
#
# THIS IS A BURN-DOWN LIST, NOT A PERMISSION SLIP.
#
# These are the malformed citations that already exist on the current tree.
# They are recorded here only so that the (non-strict) CI run does not fail on
# pre-existing debt while the debt is worked down. It is NOT a licence to add
# more:
#
#   * `check_citations.py --strict` fails on ANY finding, baselined or not.
#   * the non-strict run fails on any finding that is NOT listed below, so a
#     NEW violation still fails the build.
#
# When one of these is fixed, delete its line. Do not add lines to make a new
# failure pass -- fix the citation instead.
#
# Entries are "<file>:<line> <CHECK>". The line numbers are the location of
# the reference container (the <p class="ref"> or <div class="ref-item">).
#
# The authoritative list is the uncommented lines at the END of this file, one
# per baselined finding. It is deliberately NOT duplicated in a comment: a second
# copy is a second thing to drift, and this one did -- by 2026-09-15 three of the
# five entries that used to sit above this note described findings already fixed
# and deleted from the list below (why-this-works.html:35, :44, :58), so the file
# advertised five known findings over a two-line baseline. To burn an entry down,
# delete its line below; there is no comment to keep in step.
science-of-shared-breath.html:114 NO AUTHOR
shared-breathing-science.html:137 NO AUTHOR
